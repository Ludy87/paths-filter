{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": [
      "jest",
      "node"
    ]
  },
  "include": [
    "__tests__/**/*.{ts,tsx}"
  ],
  "exclude": [
    "node_modules"
  ] // overrides the exclude from the base project
}