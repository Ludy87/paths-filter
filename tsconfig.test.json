{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "types": [
      "jest",
      "node"
    ]
  },
  "include": [
    "src/**/*.ts",
    "__tests__/**/*.{ts,tsx}"
  ],
  "exclude": [
    "node_modules"
  ] // overrides the exclude from the base project
}
